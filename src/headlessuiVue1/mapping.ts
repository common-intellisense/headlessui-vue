import Combobox from './Combobox.json'
import ComboboxButton from './ComboboxButton.json'
import ComboboxInput from './ComboboxInput.json'
import ComboboxLabel from './ComboboxLabel.json'
import ComboboxOption from './ComboboxOption.json'
import ComboboxOptions from './ComboboxOptions.json'
import Dialog from './Dialog.json'
import DialogDescription from './DialogDescription.json'
import DialogOverlay from './DialogOverlay.json'
import DialogPanel from './DialogPanel.json'
import DialogTitle from './DialogTitle.json'
import Disclosure from './Disclosure.json'
import DisclosureButton from './DisclosureButton.json'
import DisclosurePanel from './DisclosurePanel.json'
import Listbox from './Listbox.json'
import ListboxButton from './ListboxButton.json'
import ListboxLabel from './ListboxLabel.json'
import ListboxOption from './ListboxOption.json'
import ListboxOptions from './ListboxOptions.json'
import Menu from './Menu.json'
import MenuButton from './MenuButton.json'
import MenuItem from './MenuItem.json'
import MenuItems from './MenuItems.json'
import Popover from './Popover.json'
import PopoverButton from './PopoverButton.json'
import PopoverGroup from './PopoverGroup.json'
import PopoverOverlay from './PopoverOverlay.json'
import PopoverPanel from './PopoverPanel.json'
import RadioGroup from './RadioGroup.json'
import RadioGroupDescription from './RadioGroupDescription.json'
import RadioGroupLabel from './RadioGroupLabel.json'
import RadioGroupOption from './RadioGroupOption.json'
import Switch from './Switch.json'
import SwitchDescription from './SwitchDescription.json'
import SwitchGroup from './SwitchGroup.json'
import SwitchLabel from './SwitchLabel.json'
import Tab from './Tab.json'
import TabGroup from './TabGroup.json'
import TabList from './TabList.json'
import TabPanel from './TabPanel.json'
import TabPanels from './TabPanels.json'
import TransitionChild from './TransitionChild.json'
import TransitionRoot from './TransitionRoot.json'

export function getPropsMap() {
  return [
    Combobox,
    ComboboxButton,
    ComboboxInput,
    ComboboxLabel,
    ComboboxOption,
    ComboboxOptions,
    Dialog,
    DialogDescription,
    DialogOverlay,
    DialogPanel,
    DialogTitle,
    Disclosure,
    DisclosureButton,
    DisclosurePanel,
    Listbox,
    ListboxButton,
    ListboxLabel,
    ListboxOption,
    ListboxOptions,
    Menu,
    MenuButton,
    MenuItem,
    MenuItems,
    Popover,
    PopoverButton,
    PopoverGroup,
    PopoverOverlay,
    PopoverPanel,
    RadioGroup,
    RadioGroupDescription,
    RadioGroupLabel,
    RadioGroupOption,
    Switch,
    SwitchDescription,
    SwitchGroup,
    SwitchLabel,
    Tab,
    TabGroup,
    TabList,
    TabPanel,
    TabPanels,
    TransitionChild,
    TransitionRoot,
  ]
}

export function getComponentsMap() {
  return [
    [Combobox, Combobox.name, `<${Combobox.name}></${Combobox.name}`],
    [ComboboxButton, ComboboxButton.name, `<${ComboboxButton.name}></${ComboboxButton.name}`],
    [ComboboxInput, ComboboxInput.name, `<${ComboboxInput.name}></${ComboboxInput.name}`],
    [ComboboxLabel, ComboboxLabel.name, `<${ComboboxLabel.name}></${ComboboxLabel.name}`],
    [ComboboxOption, ComboboxOption.name, `<${ComboboxOption.name}></${ComboboxOption.name}`],
    [ComboboxOptions, ComboboxOptions.name, `<${ComboboxOptions.name}></${ComboboxOptions.name}`],
    [Dialog, Dialog.name, `<${Dialog.name}></${Dialog.name}`],
    [DialogDescription, DialogDescription.name, `<${DialogDescription.name}></${DialogDescription.name}`],
    [DialogOverlay, DialogOverlay.name, `<${DialogOverlay.name}></${DialogOverlay.name}`],
    [DialogPanel, DialogPanel.name, `<${DialogPanel.name}></${DialogPanel.name}`],
    [DialogTitle, DialogTitle.name, `<${DialogTitle.name}></${DialogTitle.name}`],
    [Disclosure, Disclosure.name, `<${Disclosure.name}></${Disclosure.name}`],
    [DisclosureButton, DisclosureButton.name, `<${DisclosureButton.name}></${DisclosureButton.name}`],
    [DisclosurePanel, DisclosurePanel.name, `<${DisclosurePanel.name}></${DisclosurePanel.name}`],
    [Listbox, Listbox.name, `<${Listbox.name}></${Listbox.name}`],
    [ListboxButton, ListboxButton.name, `<${ListboxButton.name}></${ListboxButton.name}`],
    [ListboxLabel, ListboxLabel.name, `<${ListboxLabel.name}></${ListboxLabel.name}`],
    [ListboxOption, ListboxOption.name, `<${ListboxOption.name}></${ListboxOption.name}`],
    [ListboxOptions, ListboxOptions.name, `<${ListboxOptions.name}></${ListboxOptions.name}`],
    [Menu, Menu.name, `<${Menu.name}></${Menu.name}`],
    [MenuButton, MenuButton.name, `<${MenuButton.name}></${MenuButton.name}`],
    [MenuItem, MenuItem.name, `<${MenuItem.name}></${MenuItem.name}`],
    [MenuItems, MenuItems.name, `<${MenuItems.name}></${MenuItems.name}`],
    [Popover, Popover.name, `<${Popover.name}></${Popover.name}`],
    [PopoverButton, PopoverButton.name, `<${PopoverButton.name}></${PopoverButton.name}`],
    [PopoverGroup, PopoverGroup.name, `<${PopoverGroup.name}></${PopoverGroup.name}`],
    [PopoverOverlay, PopoverOverlay.name, `<${PopoverOverlay.name}></${PopoverOverlay.name}`],
    [PopoverPanel, PopoverPanel.name, `<${PopoverPanel.name}></${PopoverPanel.name}`],
    [RadioGroup, RadioGroup.name, `<${RadioGroup.name}></${RadioGroup.name}`],
    [RadioGroupDescription, RadioGroupDescription.name, `<${RadioGroupDescription.name}></${RadioGroupDescription.name}`],
    [RadioGroupLabel, RadioGroupLabel.name, `<${RadioGroupLabel.name}></${RadioGroupLabel.name}`],
    [RadioGroupOption, RadioGroupOption.name, `<${RadioGroupOption.name}></${RadioGroupOption.name}`],
    [Switch, Switch.name, `<${Switch.name}></${Switch.name}`],
    [SwitchDescription, SwitchDescription.name, `<${SwitchDescription.name}></${SwitchDescription.name}`],
    [SwitchGroup, SwitchGroup.name, `<${SwitchGroup.name}></${SwitchGroup.name}`],
    [SwitchLabel, SwitchLabel.name, `<${SwitchLabel.name}></${SwitchLabel.name}`],
    [Tab, Tab.name, `<${Tab.name}></${Tab.name}`],
    [TabGroup, TabGroup.name, `<${TabGroup.name}></${TabGroup.name}`],
    [TabList, TabList.name, `<${TabList.name}></${TabList.name}`],
    [TabPanel, TabPanel.name, `<${TabPanel.name}></${TabPanel.name}`],
    [TabPanels, TabPanels.name, `<${TabPanels.name}></${TabPanels.name}`],
    [TransitionChild, TransitionChild.name, `<${TransitionChild.name}></${TransitionChild.name}`],
    [TransitionRoot, TransitionRoot.name, `<${TransitionRoot.name}></${TransitionRoot.name}`],
  ]
}
